export type M0ShellCopy = {
  title: string;
  subtitle: string;
};

export function getM0ShellCopy(): M0ShellCopy {
  return {
    title: 'M0 Technical Shell',
    subtitle:
      'Bodyweight squat validation foundation ready for camera and pose work.',
  };
}
